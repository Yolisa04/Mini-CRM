REACT_APP_API_URL=http://localhost:5000/api
# Works around a known react-scripts 5 / eslint-plugin-jest version bug
# ("Environment key jest/globals is unknown") that otherwise fails npm run build.
DISABLE_ESLINT_PLUGIN=true
