# CRM Frontend

React dashboard for the Mini CRM. See the [project README](../README.md) for the full setup guide.

Quick start:

```
npm install
cp .env.example .env
npm start
```

Runs on http://localhost:3000 by default.
